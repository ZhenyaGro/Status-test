{
  "name": "status-test",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf public/scripts && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ZhenyaGro",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}